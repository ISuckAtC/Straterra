using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class U_Soldier : CombatUnit
{
    

    // Start is called before the first frame update
    void Start()
    {
        unitFoodCost = 20;
        unitName = "soldier";
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
